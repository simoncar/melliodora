name: CI
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 🏗 Setup Repo
        uses: actions/checkout@v3
      - name: Install modules
        run: yarn
      - name: Run tests
        run: yarn ci
